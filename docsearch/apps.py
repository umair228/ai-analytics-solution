from django.apps import AppConfig


class DocSearchConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "docsearch"
    verbose_name = "AI Document Search (LIMS knowledge base)"
